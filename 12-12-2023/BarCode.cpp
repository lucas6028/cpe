#include <bits/stdc++.h>
using namespace std;

#define ll long long

ll n, k, m;

void solve() {
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	vector<vector<vector<int>>> dp;
	while (cin >> n >> k >> m) {
		solve();
	}

	return 0;
}
